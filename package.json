{
  "name": "typingmind-mcp-server",
  "version": "1.0.0",
  "description": "TypingMind MCP Connector for Fly.io",
  "main": "index.js",
  "scripts": {
    "start": "PORT=$PORT npx @typingmind/mcp $AUTH_TOKEN"
  },
  "dependencies": {
    "@typingmind/mcp": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
