{
  "name": "typingmind-mcp-server",
  "version": "1.0.0",
  "scripts": {
    "start": "PORT=$PORT npx @typingmind/mcp $AUTH_TOKEN"
  },
  "dependencies": {
    "@typingmind/mcp": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
